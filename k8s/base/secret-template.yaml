apiVersion: v1
kind: Secret
metadata:
  name: developer-portal-secrets
  namespace: serviceradar-developer
type: Opaque
stringData:
  # Phoenix secret used for cookie/session signing.
  secret_key_base: replace-me
  # Optional GitHub PAT for plugin-registry API rate limits.
  # github_token: replace-me
